# Python dependencies for Neural Engine model training
#
# Install with:
#   pip install -r requirements.txt

# Core ML framework
torch>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0

# ONNX export and inference
onnx>=1.14.0
onnxruntime>=1.16.0

# macOS-specific: ONNX Runtime with CoreML support
# Only needed for testing ONNX models on macOS
onnxruntime-coreml>=1.16.0; sys_platform == 'darwin'
